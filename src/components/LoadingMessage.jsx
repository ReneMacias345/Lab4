export const LoadingMessage = () => {
    return (
      <div className="alert alert-info text-center">
        <p>Loading...</p>
        <small>Please wait while we load the data.</small> {/* Mensaje adicional opcional */}
      </div>
    );
  };